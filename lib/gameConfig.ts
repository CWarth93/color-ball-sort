import gameConfigData from './gameConfig.json';

export const gameConfig = gameConfigData;
export const logicalColors = gameConfig.logicalColors;
export const canonicalColors = gameConfig.canonicalColors;
export const jarCapacity = gameConfig.jarCapacity;
export const ballsPerColor = gameConfig.ballsPerColor;
export const jarCount = gameConfig.jarCount;
export const databaseName = gameConfig.databaseName;
export const levelsCollectionName = gameConfig.levelsCollectionName;
